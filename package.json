{
  "name": "fhe-salary-dapp",
  "version": "1.0.0",
  "description": "Real FHE Encrypted Salary dApp",
  "scripts": {
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy.js --network localhost",
    "test": "hardhat test",
    "node": "hardhat node",
    "frontend": "cd frontend && python -m http.server 3000"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.0"
  },
  "dependencies": {
    "@fhenixprotocol/contracts": "npm:@fhenixprotocol/contracts-nightly@latest",
    "@openzeppelin/contracts": "^4.9.0",
    "dotenv": "^17.2.3",
    "ethers": "^6.10.0"
  }
}
